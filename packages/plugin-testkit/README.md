# @getanbo/plugin-testkit

Installed-CLI acceptance helpers for Anbo plugins. `runInstalledAnbo` invokes only `node_modules/.bin/anbo`; `assertEventStream` validates run identity, sequencing, and terminal events.
