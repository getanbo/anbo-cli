# @getanbo/plugin-sdk

TypeScript contracts for Anbo Plugin API v1. Plugins default-export `{ descriptor, activate(context) }`, publish a static `./descriptor`, and use the host context for events, processes, HTTP, state, credentials, secrets, adapters, and paths.
