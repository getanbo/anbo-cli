# Anbo Cloud plugin

This repository contains the Anbo Cloud target plugin for the canonical
[`anbo`](https://github.com/getanbo/cli) command-line interface.

Development is happening on feature branches. The package does not provide an
`anbo` executable; command parsing and terminal output belong to the canonical
CLI.
